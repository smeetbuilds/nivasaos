/*
 * NivasaOS extension entrypoint.
 *
 * Import local extension modules here. Extension modules should import
 * registration functions from "@/lib/extension-registry" to avoid a loader
 * cycle. Example:
 *
 * import "@/plugins/whatsapp-cloud";
 */
