import { createTenantAction } from "@/app/actions";
import { requireUser, propertyScopeSql } from "@/lib/auth";
import { all } from "@/lib/db";
import { accessibleProperties } from "@/lib/data";
import PageHeader from "@/components/PageHeader"; import OpenModalButton from "@/components/OpenModalButton"; import ModalForm from "@/components/ModalForm"; import Flash from "@/components/Flash"; import Badge from "@/components/Badge"; import Empty from "@/components/Empty";
export const metadata={title:"Tenants"};
export default async function TenantsPage({searchParams}){const user=await requireUser();const properties=accessibleProperties(user);const scope=propertyScopeSql(user,"p");const rows=all(`SELECT t.*,p.name property_name, GROUP_CONCAT(u.name, ', ') units FROM tenants t JOIN properties p ON p.id=t.property_id LEFT JOIN lease_tenants lt ON lt.tenant_id=t.id LEFT JOIN leases l ON l.id=lt.lease_id AND l.status='active' LEFT JOIN units u ON u.id=l.unit_id WHERE ${scope.clause} GROUP BY t.id ORDER BY t.full_name`,scope.params);const query=await searchParams;return <><Flash searchParams={query}/><PageHeader eyebrow="Residents" title="Tenants" description="Maintain contact, identity, emergency, and occupancy details for every resident." actions={<OpenModalButton target="tenant-modal">Add tenant</OpenModalButton>}/>{rows.length?<div className="panel"><div className="table-wrap"><table><thead><tr><th>Tenant</th><th>Property / unit</th><th>Phone</th><th>Email</th><th>Emergency</th><th>Status</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><div className="person-cell"><span className="avatar">{r.full_name[0]}</span><span><strong>{r.full_name}</strong><small>{r.identity_number||"No ID recorded"}</small></span></div></td><td>{r.property_name}<small>{r.units||"Not assigned"}</small></td><td>{r.phone}</td><td>{r.email||"—"}</td><td>{r.emergency_contact||"—"}</td><td><Badge tone={r.status}>{r.status}</Badge></td></tr>)}</tbody></table></div></div>:<Empty icon="tenant" title="No tenants yet" text="Add a tenant record, then assign one or more tenants while creating a lease."/>}<form action={createTenantAction}><ModalForm id="tenant-modal" title="Add a tenant" description="Create the resident profile first; lease assignment happens separately." submitLabel="Add tenant"><div className="modal-body"><label><span>Property</span><select name="propertyId" required>{properties.map(p=><option value={p.id} key={p.id}>{p.name}</option>)}</select></label><div className="field-grid two"><label><span>Full name</span><input name="fullName" required/></label><label><span>Phone with country code</span><input name="phone" required placeholder="919876543210"/></label></div><div className="field-grid two"><label><span>Email</span><input type="email" name="email"/></label><label><span>Identity number</span><input name="identityNumber"/></label></div><label><span>Emergency contact</span><input name="emergencyContact" placeholder="Name · phone"/></label><label><span>Permanent address</span><textarea name="address" rows="3"/></label><label><span>Status</span><select name="status"><option>active</option><option>prospect</option><option>former</option></select></label></div></ModalForm></form></>}
