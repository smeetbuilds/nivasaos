import { createTeamMemberAction,toggleUserAction } from "@/app/actions"; import { requireRole } from "@/lib/auth"; import { all } from "@/lib/db"; import { accessibleProperties } from "@/lib/data"; import { dateLabel } from "@/lib/format"; import PageHeader from "@/components/PageHeader"; import OpenModalButton from "@/components/OpenModalButton"; import ModalForm from "@/components/ModalForm"; import Flash from "@/components/Flash"; import Badge from "@/components/Badge";
export const metadata={title:"Team"};
export default async function TeamPage({searchParams}){const user=await requireRole(['owner']);const properties=accessibleProperties(user);const members=all(`SELECT u.id,u.name,u.email,u.role,u.status,u.created_at,GROUP_CONCAT(p.name, ', ') property_names FROM users u LEFT JOIN user_properties up ON up.user_id=u.id LEFT JOIN properties p ON p.id=up.property_id GROUP BY u.id ORDER BY CASE u.role WHEN 'owner' THEN 0 WHEN 'admin' THEN 1 ELSE 2 END,u.name`);const query=await searchParams;return <><Flash searchParams={query}/><PageHeader eyebrow="Access control" title="Team & roles" description="Owners have full portfolio control. Admin and staff accounts are restricted to assigned properties." actions={<OpenModalButton target="team-modal">Add team member</OpenModalButton>}/><div className="role-cards"><article><strong>Owner</strong><span>Full system, settings, and team control.</span></article><article><strong>Admin</strong><span>Manage assigned properties and operational data.</span></article><article><strong>Staff</strong><span>Day-to-day tenant, invoice, payment, and maintenance work.</span></article></div><div className="panel"><div className="table-wrap"><table><thead><tr><th>User</th><th>Role</th><th>Property scope</th><th>Created</th><th>Status</th><th></th></tr></thead><tbody>{members.map(m=><tr key={m.id}><td><div className="person-cell"><span className="avatar">{m.name[0]}</span><span><strong>{m.name}</strong><small>{m.email}</small></span></div></td><td><Badge tone={m.role}>{m.role}</Badge></td><td>{m.role==='owner'?'All properties':m.property_names||'No properties assigned'}</td><td>{dateLabel(m.created_at.slice(0,10))}</td><td><Badge tone={m.status}>{m.status}</Badge></td><td>{m.role!=='owner'&&<form action={toggleUserAction}><input type="hidden" name="userId" value={m.id}/><button className={`text-button ${m.status==='active'?'danger':''}`}>{m.status==='active'?'Disable':'Enable'}</button></form>}</td></tr>)}</tbody></table></div></div><form action={createTeamMemberAction}><ModalForm id="team-modal" title="Add a team member" description="Create their login and choose exactly which properties they can access." submitLabel="Create account"><div className="modal-body"><div className="field-grid two"><label><span>Name</span><input name="name" required/></label><label><span>Role</span><select name="role"><option value="staff">Staff</option><option value="admin">Admin</option></select></label></div><label><span>Email</span><input type="email" name="email" required/></label><label><span>Temporary password</span><input type="password" name="password" minLength="10" required/><small>Share it securely and ask the user to use a unique password.</small></label><label><span>Assigned properties</span><select name="propertyIds" multiple size="5">{properties.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><small>Use Ctrl/Cmd to select more than one.</small></label></div></ModalForm></form></>}
