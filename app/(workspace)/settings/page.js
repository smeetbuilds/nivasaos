import { updateSettingsAction,changePasswordAction } from "@/app/actions"; import { requireUser } from "@/lib/auth"; import { all } from "@/lib/db"; import { extensions } from "@/lib/extensions"; import PageHeader from "@/components/PageHeader"; import Flash from "@/components/Flash"; import Badge from "@/components/Badge";
export const metadata={title:"Settings"};
export default async function SettingsPage({searchParams}){const user=await requireUser();const rows=all("SELECT key,value FROM settings");const settings=Object.fromEntries(rows.map(r=>[r.key,r.value]));const query=await searchParams;const editable=['owner','admin'].includes(user.role);return <><Flash searchParams={query}/><PageHeader eyebrow="Workspace configuration" title="Settings" description="System-wide defaults are deliberately small; extensions can register additional settings sections."/><div className="settings-layout"><form action={updateSettingsAction} className="panel settings-form"><div className="panel-head"><div><span className="eyebrow">General</span><h2>Organisation defaults</h2></div>{!editable&&<Badge tone="staff">Read only</Badge>}</div><div className="form-stack"><label><span>Company / portfolio name</span><input name="company_name" defaultValue={settings.company_name||''} disabled={!editable}/></label><div className="field-grid two"><label><span>Default currency</span><select name="default_currency" defaultValue={settings.default_currency||'INR'} disabled={!editable}><option>INR</option><option>USD</option><option>GBP</option><option>EUR</option><option>AED</option><option>AUD</option></select></label><label><span>Timezone</span><input name="timezone" defaultValue={settings.timezone||'Asia/Kolkata'} disabled={!editable}/></label></div><label><span>WhatsApp rent reminder template</span><textarea name="whatsapp_template" rows="5" defaultValue={settings.whatsapp_template||''} disabled={!editable}/><small>Available variables: {'{tenant}'}, {'{invoice}'}, {'{balance}'}, {'{due_date}'}.</small></label>{editable&&<button className="button primary" type="submit">Save settings</button>}</div></form><aside className="panel extension-panel"><div className="panel-head"><div><span className="eyebrow">Architecture</span><h2>Extension surfaces</h2></div></div><div className="extension-list"><div><strong>Payment methods</strong><span>Add collection methods or gateway adapters.</span></div><div><strong>Notification drivers</strong><span>Replace click-to-chat with WhatsApp Cloud API, email, or SMS.</span></div><div><strong>Dashboard sections</strong><span>Register portfolio-specific cards and operational views.</span></div><div><strong>Settings sections</strong><span>Mount configuration without modifying core screens.</span></div></div><p className="code-hint">Extension entrypoint: <code>plugins/index.js</code></p></aside><form action={changePasswordAction} className="panel settings-form"><div className="panel-head"><div><span className="eyebrow">Security</span><h2>Change your password</h2></div></div><div className="form-stack"><label><span>Current password</span><input type="password" name="currentPassword" autoComplete="current-password" required/></label><label><span>New password</span><input type="password" name="newPassword" minLength="10" autoComplete="new-password" required/></label><label><span>Confirm new password</span><input type="password" name="confirmPassword" minLength="10" autoComplete="new-password" required/></label><button className="button secondary" type="submit">Update password</button></div></form></div>{extensions.settingsSections.map(section=>{const Section=section.render;return Section?<Section key={section.id} user={user} settings={settings}/>:null})}</>}
